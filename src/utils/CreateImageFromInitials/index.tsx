import Canvas from 'react-native-canvas';

export const createImageFromInitials = (props: {
  size: number;
  name: string;
  color: string;
  background: string;
  fontSize?: number;
}) => {
  return {};
};
